"""Addendum D — client-facing availability calendar (publisher side).

D1: reduce Cal.com probe-event-type open slots to a per-day {available?} boolean over a
rolling horizon and render availability.json in the exact contract the static page reads
(see ../../availability-front-end-design/README.md). D2 adds the R2 upload.
"""
